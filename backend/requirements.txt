# --- Web framework ---
fastapi
uvicorn[standard]
python-multipart          # required for FastAPI UploadFile (exif_extractor /upload endpoint)

# --- Config / env ---
python-dotenv
pydantic-settings

# --- Databases ---
sqlalchemy
psycopg2-binary
neo4j

# --- Tool: exif_extractor ---
Pillow
requests

# --- Tool: haveibeenpwned ---
# (uses `requests`, already listed above)

# --- Tool: sherlock / maigret ---
# These are CLI tools invoked via subprocess (settings.sherlock_cmd / settings.maigret_cmd),
# NOT imported as Python libraries, but they must be installed in the same environment
# so the `sherlock` / `maigret` commands exist on PATH.
sherlock-project
maigret

instaloader
httpx


# --- Tool: audio_analysis ---
setuptools           # tensorflow/tensorflow_hub import pkg_resources internally; not in venv by default on newer Python
faster-whisper
librosa
soundfile
imageio-ffmpeg      # bundles a static ffmpeg binary in the venv, no system install needed
tensorflow
tensorflow-hub
numpy

# --- Tool: video_analysis ---
torch
torchvision
ultralytics
transformers
pytesseract
# geopy

langchain-ollama
langgraph